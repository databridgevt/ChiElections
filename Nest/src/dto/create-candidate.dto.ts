// This class is set up in almost exactly
// the same way as the election dto, for
// exactly the same reasons

export class CreateCandidateDTO {
  name: string;
  party: string;
  office: string;
}
